<p id="notice"><%= notice %></p>

<h1><u>Kari's To Do List</u></h1>

<table>
  <thead>
    <tr>
      <th><u>Title</u></th>
      <th><u>Notes</u></th>
      <th><u>Due</u></th>
      <th><u>Done</u></th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @to_dos.each do |to_do| %>
      <tr>
        <td><%= to_do.title %></td>
        <td><%= to_do.notes %></td>
        <td><%= to_do.due %></td>
        <td><%= to_do.done %></td>
        <td><%= link_to 'Show', to_do %></td>
        <td><%= link_to 'Edit', edit_to_do_path(to_do) %></td>
        <td><%= link_to 'Destroy', to_do, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New To do', new_to_do_path %>
